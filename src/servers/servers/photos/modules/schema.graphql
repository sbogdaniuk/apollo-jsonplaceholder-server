type Query {
  _empty: String @deprecated(reason: "Dummy field")
}

type Mutation {
  _empty: String @deprecated(reason: "Dummy field")
}

type Photo {
  albumId: ID!
  id: ID!
  title: String!
  url: String!
  thumbnailUrl: String!
}
